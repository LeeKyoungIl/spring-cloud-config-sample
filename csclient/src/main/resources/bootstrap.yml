server:
  port: 8094

spring:
  application:
    name: csclient
  cloud:
    config:
      uri: http://localhost:8093

endpoints:
  refresh:
    enabled: true

management:
  security:
    enabled: false